<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{genre}} Books - Book Heaven</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  
  <!-- Custom CSS for Genre Page -->
  <link rel="stylesheet" href="/css/genre-page.css">
</head>
<body>
  <!-- Genre Page Header -->
  <div class="genre-header text-center">
    <h1 class="display-3">{{genre}} Books</h1>
    <p class="lead">Explore the best books in the {{genre}} genre!</p>
  </div>

  <!-- Books Section -->
  <div class="container mt-4">
    <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 row-cols-lg-5 g-3">
      {{#each books}}
      <div class="col mb-4">
        <div class="card h-100 genre-book-card">
          <img src="{{this.image}}" class="card-img-top img-fluid" alt="{{this.title}} cover" onerror="this.src='/images/default-book.png';">
          <div class="card-body">
            <h5 class="card-title">{{this.title}}</h5>
            <p class="card-text">
              <strong>Author:</strong> {{this.authors}}<br>
              <strong>Published:</strong> {{this.publishedDate}}
            </p>
            <a href="{{this.infoLink}}" class="btn btn-primary btn-sm" target="_blank">More Info</a>
          </div>
        </div>
      </div>
      {{/each}}
    </div>
  </div>

  <!-- Pagination Section -->
  <nav aria-label="Page navigation" class="mt-4">
    <ul class="pagination justify-content-center">
      <!-- Previous Page Arrow -->
      {{#if hasPrevPage}}
        <li class="page-item">
          <a class="page-link arrow" href="?page={{prevPage}}" aria-label="Previous">
            <span aria-hidden="true">&laquo;</span>
          </a>
        </li>
      {{/if}}

      <!-- Page Numbers -->
      {{#each pages}}
        <li class="page-item {{#if isActive}}active{{/if}}">
          <a class="page-link" href="?page={{this}}">{{this}}</a>
        </li>
      {{/each}}

      <!-- Next Page Arrow -->
      {{#if hasNextPage}}
        <li class="page-item">
          <a class="page-link arrow" href="?page={{nextPage}}" aria-label="Next">
            <span aria-hidden="true">&raquo;</span>
          </a>
        </li>
      {{/if}}
    </ul>
  </nav>

  <!-- Footer -->
  <footer class="text-center mt-5">
    <p>&copy; 2024 The Book Heaven. All rights reserved.</p>
  </footer>

  {{!-- <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script> --}}
</body>
</html>
