<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{genre}} Books</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <style>
    .book-card {
      width: 18rem;
      margin-bottom: 20px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }
    .book-card img {
      max-height: 300px;
      object-fit: cover;
    }
    .genre-title {
      text-align: center;
      margin-top: 20px;
      margin-bottom: 40px;
    }
  </style>
</head>
<body>
  <div class="container mt-5">
    <div class="genre-title">
      <h1>Top {{genre}} Books</h1>
      <p>Explore the most popular books in the {{genre}} genre.</p>
    </div>
    
    <div class="row">
      {{#each books}}
        <div class="col-md-4">
          <div class="card book-card">
            <img src="{{this.volumeInfo.imageLinks.thumbnail}}" class="card-img-top" alt="{{this.volumeInfo.title}} cover">
            <div class="card-body">
              <h5 class="card-title">{{this.volumeInfo.title}}</h5>
              <p class="card-text">{{this.volumeInfo.description}}</p>
              <a href="{{this.volumeInfo.infoLink}}" target="_blank" class="btn btn-primary">More Info</a>
            </div>
          </div>
        </div>
      {{/each}}
    </div>
  </div>

  {{!-- <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script> --}}
  {{!-- <script src="https://cdn.jsdelivr.net/npm/popper.js@1.14.7/dist/umd/popper.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script> --}}
</body>
</html>
