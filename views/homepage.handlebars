<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Welcome to Book Heaven</title>
  <!-- Bootstrap 5.3.3 CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <!-- Add custom CSS for glowing effect -->
  <style>
    .glow-box {
      border: 2px solid #00ff99;
      box-shadow: 0 0 15px #00ff99;
      transition: all 0.3s ease-in-out;
    }

    .glow-box:hover {
      box-shadow: 0 0 30px #00ff99;
    }

    .book-container {
      display: flex;
      justify-content: space-around;
      margin-top: 30px;
    }

    .book-card {
      width: 30%;
      padding: 20px;
      text-align: center;
      border-radius: 8px;
      background-color: #f9f9f9;
      overflow-y: auto; /* Enable scroll for long content */
      max-height: 300px;
    }

    .book-card img {
      max-width: 100%;
      height: auto;
    }

    .review-section {
      margin-top: 20px;
    }

    .review-card {
      border: 1px solid #ddd;
      padding: 15px;
      margin-bottom: 10px;
    }
  </style>
</head>

<body>
  <div class="container mt-5">
    <div class="jumbotron text-center">
      <h1 class="display-4">Welcome to Book Heaven</h1>
      <p class="lead">Discover new books, connect with readers, and join the conversation!</p>
      {{#if logged_in}}
        <p class="lead">You are logged in as <strong>{{name}}</strong>.</p>
      {{/if}}
    </div>
    
    <!-- Top Books Section -->
    <div class="text-center mt-4">
      <h2>Top Books Right Now</h2>
      <p>Here are some of the most popular books currently available:</p>
    </div>

    <!-- Top Books with Glowing Boxes -->
    <div class="book-container">
      {{#each googleBooks}}
      <div class="book-card glow-box">
        <img src="{{this.volumeInfo.imageLinks.thumbnail}}" alt="{{this.volumeInfo.title}} cover">
        <h5>{{this.volumeInfo.title}}</h5>
        <p><strong>Author:</strong> {{this.volumeInfo.authors}}</p>
        <p>{{this.volumeInfo.description}}</p>

        <!-- Display reviews for each book -->
        <div class="review-section">
          <h6>Reviews:</h6>
          {{#each ../reviewsForBooks.[this.volumeInfo.title]}}
          <div class="review-card">
            <p>{{this.comment}}</p>
            <small><strong>Reviewed by:</strong> {{this.user.name}}</small>
          </div>
          {{else}}
          <p>No reviews found for this book.</p>
          {{/each}}
        </div>
      </div>
      {{/each}}
    </div>
  </div>

  <!-- Bootstrap JS and dependencies -->
  {{!-- <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script> --}}
  {{!-- <script src="https://cdn.jsdelivr.net/npm/popper.js@1.14.7/dist/umd/popper.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script> --}}
</body>
</html>
